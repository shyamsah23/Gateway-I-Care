spring:
  application:
    name: Gateway-I-Care

#  user service routes
  cloud:
    gateway:
      routes:
        - id: user-service
          uri: ${USER_SERVICE_PATH}
          predicates:
            - Path=/auth/**

server:
  port: 8081

secret:
  header:
    key: ${SECRET_HEADER_KEY}
